[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "cs_reqs"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
exclude = ["prolog*", ".venv*"]
